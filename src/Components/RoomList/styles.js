import styled from "@emotion/styled";
import {List, ListItem} from "@chakra-ui/react";

export const StyledRoom = styled(List)`
  flex-grow: 1;
  flex-direction: column;
  display: flex;

`;
export const StyledListItem = styled(ListItem)``;